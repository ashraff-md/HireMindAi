/** Compatibility barrel — brief naming (`startInterview`, `sendAnswer`, `getFeedback`). */
export {
  mockStartInterview as startInterview,
  mockSendAnswer as sendAnswer,
  mockGetFeedback as getFeedback,
} from "@/lib/mock-api";
